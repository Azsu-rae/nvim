

# Things that bother me

- `i_<C-s>` doesn't work with java and does show function signatures even though `k` does perfectly normally
- `-- INSERT --` hides messages in insert mode (amongst which are the `no signature help available` from the previous nuisance)
